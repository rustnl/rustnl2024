---
const { name, image, description, bio, linkedin, twitter, mastodon, github } = Astro.props;
---
<div class="speaker">
    <div class="space-y-6">
        <img src={`/images/speakers/${image}`} alt="" />
        <div>
            <h4 class="font-light text-lg leading-tight">{name}</h4>
            <h3>{description}</h3>
        </div>
        {
            bio && (
                <p>{bio}</p>
            )
        }
        <div class="socials">
            {linkedin && <a href={linkedin}>LinkedIn</a>}
            {twitter && <a href={twitter}>X</a>}
            {mastodon && <a href={mastodon}>Mastodon</a>}
            {github && <a href={github}>GitHub</a>}
        </div>
    </div>
    
</div>
